const root = `/sandbox/v2`
export enum PathRoute {
  SANDBOX = `${root}/sandbox`,
  HOME = `${root}/`,
  LOGIN = `${root}/login/:expired?`,
  SIGNUP = `${root}/signup`,
  RESET_PASSWORD = `${root}/reset-password`,
  PAYMENTS_TOKENS = `${root}/payments-tokens`,
  SECURITIES_TOKENS = `${root}/securities-tokens`,
  SOROBAN_SMART_CONTRACTS = `${root}/soroban-smart-contracts`,
  BLOCKCHAIN_EXPLORER = `/blockchain-explorer`,
  SETTINGS = `${root}/settings/team-members`,
  PERMISSIONS = `${root}/settings/permissions`,
  ROLES_MANAGE = `${root}/settings/roles_manage`,
  PROFILE = `${root}/profile`,
  FORGE_ASSET = `${root}/forge-asset`,
  ASSET_HOME = `${root}/asset-management/home`,
  MINT_ASSET = `${root}/asset-management/mint`,
  BURN_ASSET = `${root}/asset-management/burn`,
  DISTRIBUTE_ASSET = `${root}/asset-management/distribute`,
  AUTHORIZE_ACCOUNT = `${root}/asset-management/authorize`,
  CLAWBACK_ASSET = `${root}/asset-management/clawback`,
  FREEZE_ACCOUNT = `${root}/asset-management/freeze`,
  VAULTS = `${root}/vaults`,
  VAULT_CREATE = `${root}/vault-create`,
  VAULT_DETAIL = `${root}/vault-detail`,
  CONTRACT_DETAIL = `${root}/contract-detail`,
  CONTRACT_CREATE = `${root}/contract-create`,
  TOKEN_MANAGEMENT = `${root}/token-management`,
  DASHBOARDS = `${root}/dashboards`,
}
